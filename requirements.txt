# Layer 1 — predictor
pandas
scikit-learn
requests
python-dotenv

# Layer 2 — Claude agent analyst + persona
anthropic

# Layer 3 — voice (HERO)
deepgram-sdk

# Web UI — FastAPI backend + browser frontend
fastapi
uvicorn[standard]
python-multipart  # parse multipart audio uploads on /ask

# Stretch — live data + market odds via Browserbase (remote headless browser)
httpx             # Browserbase Sessions REST API
playwright        # drives the REMOTE Browserbase browser over CDP (no local browser install needed)

# Stretch — bracket visual (uncomment if you build it)
# streamlit        # fastest visual UI
